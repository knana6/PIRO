# Web
Django==5.2.4
django-allauth==65.10.0

# Env (둘 중 하나만 쓰세요)
python-dotenv==1.1.1
# django-environ==0.12.0

# DB
psycopg2-binary==2.9.10
pgvector==0.4.1

# LLM / Agent
tiktoken==0.9.0
langchain==0.3.27
langchain-community==0.3.28
langchain-core==0.3.75
langchain-text-splitters==0.3.9
langgraph==0.6.3
langgraph-checkpoint==2.1.1
langgraph-prebuilt==0.6.3
langgraph-sdk==0.2.0
langsmith==0.4.10

# Vector / RAG
faiss-cpu==1.11.0.post1

# HF / Local inference
torch==2.9.1
transformers==4.57.3
accelerate==1.12.0
huggingface-hub==0.36.0
safetensors==0.7.0
tokenizers==0.22.1

# Utils (직접 쓰는 경우만 유지)
requests==2.32.5
pydantic==2.11.7
pydantic-settings==2.10.1
orjson==3.11.1

# Data (정말 쓰면 유지, 안 쓰면 삭제)
numpy==2.3.2
pandas==2.3.1

# Image (이미지 업로드/처리 쓰면 유지)
Pillow